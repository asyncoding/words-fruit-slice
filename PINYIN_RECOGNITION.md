# 拼音手写识别技术方案对比

> 生成日期：2026-07-31
> 对比对象：`words-fruit-slice`（当前项目） vs `grade1-practice/game`（兄弟项目）

---

## 一、words-fruit-slice 当前技术方案（本项目）

### 1.1 整体架构

**纯前端单机方案**：无后端、无外部 API。单文件 `index.html`（约 1400 行），依赖本地 vendor 的 onnxruntime-web + jieba-wasm。数据全部打包在 `game-data.js`。

### 1.2 识别流程（完整链路）

```
孩子书写（canvas 360×420，pointer 事件，白笔透明底）
  → 停笔 900ms（hwRecTimer 单一滑动定时器：pointerup 重置 / pointerdown 取消）
  → recognizeLetterCNN()
      ├─ 1. 墨迹检测：alpha>20 取 bbox；无墨 → 提示重写
      ├─ 2. 预处理（EMNIST 质心对齐，镜像 EMNIST 官方 pipeline）：
      │     bbox → 等比缩放至 20×20 → 28×28 画布居中
      │     → 笔画像素质心对齐到 (14,14)（整数位移 + 画布内钳制）
      ├─ 3. 推理：onnxruntime-web（本地 vendor/ort，wasm threaded，模型 2.54MB）
      ├─ 4. 候选集约束：目标拼音去重字母集合内取最大概率
      │     例：目标"ru" → 候选 {r,u}；目标"shuang" → {s,h,u,a,n,g}
      ├─ 5. 拒识策略（HW_MIN_CONF = 0.20）：
      │     候选内 max ≥ 0.20 → 采纳该字母（上屏）
      │     候选内 max < 0.20 → lowConf，回退全 26 类全局最大 + 标记低置信 → 拒识
      └─ 6. 反馈：拒识 → "没看清，请重写"（已识别字母保留）；引擎未就绪/失败 → 明确文案
```

### 1.3 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 画布 | 360×420 | 手机竖屏适配 |
| 笔宽 | `max(8, min(w,h)*0.05)` ≈ 18px | 匹配 EMNIST 2.5px/28×28 粗细 |
| 停笔窗口 | 900ms 单一滑动定时器 | 多笔画字母（t/i/f/k/u/g）不被拆碎 |
| 候选集 | 目标拼音去重字母集 | 消除候选外干扰类 |
| 拒识阈值 | 0.20（迭代5 从 0.30 调优） | 离线 624 样本网格寻优，+3.5% 通过率 |
| 模型 | 26 类 EMNIST CNN（2 conv + 2 fc，63 万参数） | emnist_cnn.onnx 2.54MB |
| 引擎加载 | 本地 vendor（3s 内） | CDN 被墙（jsdelivr 164s+ 失败）已禁 |

### 1.4 训练管线（tools/train/train.py）

| 数据源 | 规模 | 说明 |
|--------|------|------|
| EMNIST Letters 训练集 | 124,800 | 成人手写真实分布 |
| StrokeDataset 笔画渲染 | 158,370（含弱字母×2 过采样） | EMNIST 骨架 → 圆头笔画渲染（镜像 app 绘制管线），修复 sim-to-real 缺口 |
| 字体合成 | 4 字体 × 16 变体 × 24 重复 | Arial/Georgia/Chalkboard/Bradley Hand |

- 增强：KidAugment（±25° 旋转、0.75~1.25 缩放、±3px 平移、笔画擦除、粗细腐蚀膨胀、弯曲/闭合/连笔变形）
- 训练：Adam lr 1e-3 → cosine，8 epoch 强增强 + 3 epoch 低增强微调，MPS（约 11 分钟）
- 导出：BN 折叠 → ONNX opset 13（规避 torch 导出器 BN 折叠 bug）

### 1.5 验收指标（当前实测）

| 场景 | 结果 |
|------|------|
| 624 真手写笔画回放（全量，单字母候选集最严苛） | **93.1%**，拒识 43，错识 0 |
| 浏览器同口径 312 子集（迭代4→5） | 88.14% → **91.67%** |
| 真实停顿 700ms 端到端（ru/you/shuang） | 全部通过（g 拒识 2 次后重试成功） |
| 模型加载 | 本地 3s（CDN 版 164s 失败） |

### 1.6 已排除的方向（实验记录）

- 候选集归一化置信度：与绝对阈值等价，弃用
- 重训（hard-example mining × 4 轮）：test 泄漏 / 分布歪斜 / Focal 塌缩，全部失败——当前数据下 92.82% test 是平台期，提升靠新数据（真孩子笔迹）

---

## 二、grade1-practice/game 技术方案（兄弟项目）

### 2.1 整体架构

**前端 + Node 后端**：`game/public/`（前端 app.js 约 2020 行）+ `game/server.js`（百度 OCR API 代理）。前端识别优先走本地，失败兜底走后端百度 API。

### 2.2 识别流程（完整链路）

```
书写（停笔 1.2s）
  → recognize() 主流程
      ├─ 1. CNN 识别（前端 onnxruntime-web，CDN 加载 ort）：
      │     预处理：bbox + 15% 内边距 → 方形缩放到 28×28 黑底 → 灰度（无质心对齐）
      │     → 全 26 类 top-1 全局最大概率（无候选集、无拒识阈值）
      │     → 预期拼音兜底：期望字母在 top3 且 score>0.15 → 直接采用预期字母
      │     → 否则 confidence ≥ 0.5 采纳 CNN top-1
      ├─ 2. 几何识别兜底（recognizePinyinByGeometry，像素分析）：
      │     连通分量 / 闭环检测 / 中心交叉检测，覆盖 26 字母
      │     → 置信度 ≥ 0.55 采纳
      ├─ 3. 预期拼音强采纳：CNN+几何都无结果，但几何能返回任意结果
      │     → 直接采用期望字母（不校验置信度）
      ├─ 4. 百度 OCR 最终兜底（/api/handwrite）：
      │     裁剪 + 白底黑字 → 百度手写 OCR + 通用 OCR(ENG) 并行
      │     限流（code 18）自动重试 3 次
      │     → fixPinyinText() 映射修正（0→o, I→l, ×→x, 1/9 上下文消歧）
      │     → 百度空结果 → 几何再兜底
      └─ 5. 失败提示：限流 / 服务错误 / "写大一点再试"
```

### 2.3 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 停笔窗口 | 1.2s 固定定时器 | 无滑动取消逻辑 |
| 候选集 | 无 | 全 26 类 top-1 |
| 拒识阈值 | 无（conf≥0.5 即上屏） | 预期拼音 >0.15 即强采纳 |
| CNN 预处理 | bbox+15% 内边距方形缩放 | 旧方案（无质心对齐） |
| 后端 | 百度 OCR（手写 + 通用 ENG） | token 缓存 + 重试 3 次 |
| 几何识别 | 26 字母像素特征 | 已知 p/r、s、g 检测缺陷 |
| 引擎加载 | jsdelivr CDN（未本地化） | 国内可能被墙/慢 |
| 训练 | `train_emnist.py`：纯 EMNIST Letters 124k，30 epoch CPU，torchvision 基础增强 | 无笔画渲染数据、无字体合成 |

### 2.4 验收

- Playwright 自动化 + 测试用例（26 单字母 + 拼音组合 + 带声调 + 边缘用例）
- 目标：单字母 ≥99%，拼音 ≥99.9%，端到端 ≥99.99%（理想目标，未达成）

---

## 三、两者的区别

### 3.1 架构差异

| 维度 | words-fruit-slice | grade1-practice/game |
|------|-------------------|----------------------|
| 部署形态 | 纯前端单机（无后端） | 前端 + Node 后端（百度 OCR） |
| 网络依赖 | 零（全部本地 vendor） | 百度 API（限流/成本/断网即失效） |
| 代码组织 | 单文件 index.html | app.js + server.js + 工具链 |
| 离线可用 | ✅ | ❌（识别兜底依赖后端） |

### 3.2 识别策略差异（核心）

| 维度 | words-fruit-slice | grade1-practice/game |
|------|-------------------|----------------------|
| 主识别器 | CNN 单字母 | CNN + 几何识别双路 |
| 兜底 | 无（拒识提示重写） | 预期拼音强采纳 + 百度 OCR |
| 候选集约束 | ✅ 目标拼音字母集 | ❌ 全局 top-1 |
| 拒识机制 | ✅ 阈值 0.20 + lowConf | ❌ 无（0.5 即上屏，预期字母 0.15 即强采纳） |
| 多音字上下文 | 候选集（目标拼音字母） | expectedLetter 前置校验（top3 命中） |
| 误上屏防护 | 强（候选集外干扰类天然排除） | 弱（全局 top-1 + 强采纳可能错上屏） |
| 拒识体验 | 重写提示（已识别字母保留） | 多路兜底尽量出字（宁可错不可空） |

### 3.3 预处理差异

| 维度 | words-fruit-slice | grade1-practice/game |
|------|-------------------|----------------------|
| 对齐 | bbox→20×20→28×28 居中→**质心对齐 (14,14)** | bbox+15% 内边距→方形缩放 28×28 |
| 背景 | 透明底白笔画 | 黑底白笔画 |
| 依据 | EMNIST 官方 pipeline 镜像 | 旧式方形缩放 |

质心对齐是 EMNIST 官方训练时的标准对齐方式，直接决定模型输入分布匹配度——words-fruit-slice 的预处理与训练/推理完全一致，grade1 的预处理与其 30 epoch 训练的增强分布并不严格对齐。

### 3.4 训练数据差异

| 维度 | words-fruit-slice | grade1-practice/game |
|------|-------------------|----------------------|
| EMNIST | 124,800 | 124,000（同源） |
| 笔画渲染 | ✅ 158,370（镜像 app 管线） | ❌ 无 |
| 字体合成 | ✅ 4 款 | ❌ 无 |
| 增强 | KidAugment 强增强（±25°等） | torchvision 基础变换 |
| 训练时长 | ~11 分钟（MPS） | 5-10 分钟（CPU 30 epoch） |
| sim-to-real | ✅ 直接解决（笔画渲染数据） | ❌ 依赖纯位图分布 |

### 3.5 部署可靠性差异

| 维度 | words-fruit-slice | grade1-practice/game |
|------|-------------------|----------------------|
| ort 加载 | 本地 vendor（3s） | jsdelivr CDN（**国内被墙风险，迭代4 实测 164s+ 失败**） |
| 引擎失败处理 | 明确提示"识别引擎加载中/失败" | 静默跳过（console warn），走兜底 |
| 停笔时序 | 900ms 滑动定时器（防碎笔） | 1.2s 固定（多笔画字母有拆碎风险） |

### 3.6 实测水平差异

| 指标 | words-fruit-slice | grade1-practice/game |
|------|-------------------|----------------------|
| 同口径回放（624 真手写笔画，单字母） | **93.1%**（拒识 43，错识 0） | 未公开测试报告，存 IMG_*.JPG 真机截图 |
| 端到端 | ru/you/shuang 真实停顿全过 | Playwright 模拟 + 真机截图 |
| 拒识 vs 错识 | 宁可拒识不误上屏 | 宁可出字不拒识（预期字母强采纳，错字风险高） |

### 3.7 核心结论

1. **words-fruit-slice 的优势**：零网络依赖（国内可用性）、候选集约束（错识天然防护）、拒识机制（错识=0）、预处理与训练对齐（质心对齐）、训练数据含笔画渲染（sim-to-real）、本地引擎（3s 加载）。
2. **grade1-practice/game 的优势**：多路兜底（几何+百度 OCR）降低"空结果"概率、"预期拼音"强采纳提高游戏通关率、有 Playwright 自动化测试体系、带声调处理（TONE_MAP）。
3. **互相借鉴点**：
   - words-fruit-slice 可借鉴：Playwright 验收体系、带声调输入处理、多路兜底的"空结果率"指标
   - grade1-practice/game 可借鉴：ort 本地化（CDN 被墙）、质心对齐预处理、候选集约束、笔画渲染训练数据、拒识阈值（强采纳是"实测非常差"的重要嫌疑——错上屏比拒识更伤害体验）
4. **grade1 实测"非常差"的高概率根因**（按可能性排序）：
   a. ort CDN 加载失败/极慢（jsdelivr 被墙）→ CNN 静默不可用（words-fruit-slice 迭代4 已验证此坑）
   b. 无候选集约束 + 无拒识 → 相似字母（i/l、g/q、d/o）高概率错上屏
   c. 预处理不匹配（无质心对齐）+ 训练数据不含笔画渲染 → 真实笔画输入分布偏移
   d. 预期字母强采纳（>0.15 即采用）→ 写错字母时系统自作主张上屏错误预期
