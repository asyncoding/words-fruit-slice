# 验收日志 — 拼音识别优化（fc5d0f2 候选集约束 + 置信度拒识 + 前缀反馈）

## 测试信息

| 项 | 值 |
|---|---|
| 测试时间 | 2026-07-31（验收 Agent 自动化执行） |
| 被测版本 | commit fc5d0f2（拼音识别候选集约束+置信度拒识+前缀反馈） |
| 验收依据 | AGENT.md §4（4.2 指标 / 4.3 用例） |
| 环境 | macOS Chrome headless（gstack browse）/ 本地 `npx serve` :3004 |
| 模型 | emnist_cnn.onnx（26 类，3.5MB），onnxruntime-web 1.21.0（wasm） |
| 工具 | `test/recognition-test.html`（像素级确定性测试页，镜像 index.html 预处理/推理逻辑）、browse 真浏览器冒烟 |

## 用例覆盖

| 验收点 | 方案 | 样本数 |
|---|---|---|
| 1. 单字母识别（无候选集） | 26 字母 × 6 变体（Arial/Georgia × 抖动 ±3px，110px 打印体） | 156 |
| 2. 候选集约束 | 每字母 allowed={自身, 最强混淆对} × 6 变体；补充：排除混淆对、真实拼音集 {c,h,u,n} | 156 + 补充 13 |
| 2b. 回退保底 | 每字母 allowed={两个混淆字母，不含真字母} × 6 变体 | 156 |
| 3. 拒识 | 横线/竖线/斜线/圆点/乱画 ×（无候选 + 候选集[c,h,u,n]） | 10 判定 |
| 4. 端到端冒烟 | 识字1 → L1 切 12 果（合成 swipe）→ L2 → 「春」手写 c/h/u/n | 1 生字 |
| 5. 拒识端到端 + 前缀反馈 | 「冬」画横线 / 写错字母 x | 2 用例 |

## 通过 / 失败统计

| 验收点 | 结果 | 判定 |
|---|---|---|
| 1. 单字母准确率 | **86.54%**（135/156）｜Arial 100%（78/78），Georgia 73.08%（57/78） | ❌ 未达 ≥90% |
| 2. 候选集约束（含混淆对） | 约束命中 86.54%（135/156）＝无候选水平（测试集含最强混淆对，天然无法提升）；排除混淆对后 i 案例 0%→100% | ⚠️ 机制正确，条件性有效 |
| 2b. 回退保底 | 候选集不含真字母时 84.62%（132/156）返回真字母；80.77% 触发 lowConf | ✅ 符合设计 |
| 3. 拒识 | **0/5 拦截**：横线→j(0.35)、竖线→l(0.84)、斜线→l(0.76)、圆点→o(1.00)、乱画→h(0.70) | ❌ **拒识失效** |
| 4. 端到端通关 | c→h→u→n 逐字母识别正确 →「🎉 你真棒！拼音正确！」→ 1s 后蒙层关闭 → 卡片 .done | ✅ |
| 5. 拒识端到端 | 「冬」画横线 → 被采纳为字母 i，未出现「✍️ 没看清，再写一次」 | ❌ 复现 bug |
| 5b. 前缀反馈 | 「冬」写 x →「❌ 第1个字母不对，点字母上的✕重写」；「春」首字母错 → 同提示；前缀正确时无提示 | ✅ |
| 延迟 | 单次推理平均 **1.7ms**（目标 ≤100ms）；模型加载（缓存后）339ms | ✅ |
| ?allowed=da 快速模式 | 6 样本全部正确（无候选/候选一致，conf ≥0.93） | ✅ |

## 混淆矩阵摘要（最易混字母对）

| 排名 | 真字母 → 误识 | 次数(×6) | 置信度 | 备注 |
|---|---|---|---|---|
| 1 | h → b | 3 | 0.97 | 模型自信地错（Georgia 字形） |
| 1 | i → j | 3 | 0.59 | 候选集排除 j 后可修复 |
| 1 | k → b | 3 | 0.81 | 自信地错 |
| 1 | l → i | 3 | 0.83 | 自信地错（衬线脚像 i 的点） |
| 1 | m → a | 3 | 0.74 | 自信地错 |
| 1 | n → d | 3 | 0.80 | 自信地错 |
| 1 | r → i | 3 | 0.99 | 自信地错 |

全部 21 个失败样本均为 **Georgia 衬线体**（3 个抖动变体完全一致）；Arial 打印体 78/78 全对。真实手写（细笔画、无衬线）与 Arial 更接近，实际场景准确率预计显著高于 86.5%，但衬线/圆角字体是模型盲区。

## 失败案例分析

**案例 A（严重）：拒识完全失效 — 置信度取错位置**
- 现象：横线/竖线/圆点/乱画 5 种垃圾输入全部被采纳为字母（conf 0.35~1.00）。
- 定位：`index.html:878-881` 回退路径 `lowConf = true` 后返回的 `confidence` 是**全局 26 类最大概率**（如竖线→l 0.84），而 `doHWRecognize`（`index.html:964`）只检查 `result.confidence < HW_MIN_CONF`，**从未使用 `lowConf` 标志**。清晰笔画的全局最大概率几乎都 ≥0.30，拒识阈值形同虚设。
- 证据链：测试页 10/10 判定采纳 → E2E「冬」横线被识别为 i 并追加 chip（本应提示重写）。
- 修复方向：`doHWRecognize` 拒识条件改为 `if (result.lowConf || result.confidence < HW_MIN_CONF)`；更优做法是回退时让 `recognizeLetterCNN` 返回**候选集内最大概率**作为 confidence（此时 lowConf 必为 true），让拒识阈值真正反映「候选集内的把握」。

**案例 B（中）：回退保底可输出候选集之外的字母并被采纳**
- 现象：乱画输入 allowed=[c,h,u,n] → 返回 h(0.70, lowConf=false) 被采纳。
- 定位：allowed 内最大概率 ≥0.30 时直接采纳该字母（`index.html:875`），乱画恰好激活了 h。候选集约束只对「形似」输入有效，对「恰好命中候选集」的垃圾无能为力。
- 修复方向：与案例 A 同一处（lowConf/候选集内置信度判定），另可加多帧投票或最小笔画数启发式。

**案例 C（低）：模型对笔画粗细/字体敏感**
- 现象：bold 字重使 c→g（0.93）、72px bold 同样翻转；Georgia 全字母 21/21 失败样本中 7 个字母自信错误（0.74~0.99）。
- 定位：EMNIST 训练分布为细笔画手写；预处理仅做包围盒+缩放，无笔画粗细归一（AGENT.md 方向 A4 未实施）。
- 修复方向：方向 A4 笔画加粗/细化归一；方向 A3 孩子笔迹增强重训（尤其衬线/圆角变体）。

## 结论

| 指标（AGENT.md 4.2） | 目标 | 实测 | 达标 |
|---|---|---|---|
| 单字母准确率（规范书写） | ≥95% | Arial 口径 100%；总体 86.54%（含衬线压力测试） | ⚠️ 条件达标 |
| 端到端拼音通关 | ≥90% | 1/1 生字通过（样本少，说明性） | ⚠️ 待扩样 |
| 识别延迟 | ≤100ms | 1.7ms | ✅ |
| 无拒识误判（乱画被当正确字母） | 有阈值拦截 | **5/5 垃圾输入被采纳** | ❌ |
| 候选集约束 / 回退保底 / 前缀反馈 | 生效 | 约束条件性生效、回退符合设计、前缀反馈生效 | ✅ |

**结论：本轮优化（fc5d0f2）核心功能「置信度拒识」存在逻辑缺陷未生效，不予通过；候选集约束与前缀反馈功能验收通过。** 拒识修复（1 行条件改动）后需回归验收。

## 下一步建议

1. **必修**：修复拒识（案例 A）— 采纳 `lowConf` 或回退返回候选集内置信度；回归 5 种垃圾输入 + 真实手写。
2. 候选集约束对「模型自信错误」无效（h→b 0.97 等）——需方向 A4 笔画归一 / A3 重训才能根治，候选集只是第一道防线。
3. 端到端通关率扩样：按 AGENT.md 4.3 PINYIN_COMBO 抽 ≥10 生字（含易混淆 li/ni/lu/nu 等）跑真实浏览冒烟。
4. 拒识阈值调优时注意挫败感（AGENT.md 六-4）：建议先验证低置信分布再定 0.30 是否合适。

## 附：复现命令

```bash
./start.sh 3000   # 若端口被占自动后移
# 像素级测试：http://localhost:<port>/test/recognition-test.html
# 快速单字母：http://localhost:<port>/test/recognition-test?allowed=da
# E2E L1 自动切瓜：browse eval test/e2e-l1-slice.js
```
