# 验收日志 — 拼音识别优化（fc5d0f2 候选集约束 + 置信度拒识 + 前缀反馈）

## 测试信息

| 项 | 值 |
|---|---|
| 测试时间 | 2026-07-31（验收 Agent 自动化执行） |
| 被测版本 | commit fc5d0f2（拼音识别候选集约束+置信度拒识+前缀反馈） |
| 验收依据 | AGENT.md §4（4.2 指标 / 4.3 用例） |
| 环境 | macOS Chrome headless（gstack browse）/ 本地 `npx serve` :3004 |
| 模型 | emnist_cnn.onnx（26 类，3.5MB），onnxruntime-web 1.21.0（wasm） |
| 工具 | `test/recognition-test.html`（像素级确定性测试页，镜像 index.html 预处理/推理逻辑）、browse 真浏览器冒烟 |

## 用例覆盖

| 验收点 | 方案 | 样本数 |
|---|---|---|
| 1. 单字母识别（无候选集） | 26 字母 × 6 变体（Arial/Georgia × 抖动 ±3px，110px 打印体） | 156 |
| 2. 候选集约束 | 每字母 allowed={自身, 最强混淆对} × 6 变体；补充：排除混淆对、真实拼音集 {c,h,u,n} | 156 + 补充 13 |
| 2b. 回退保底 | 每字母 allowed={两个混淆字母，不含真字母} × 6 变体 | 156 |
| 3. 拒识 | 横线/竖线/斜线/圆点/乱画 ×（无候选 + 候选集[c,h,u,n]） | 10 判定 |
| 4. 端到端冒烟 | 识字1 → L1 切 12 果（合成 swipe）→ L2 → 「春」手写 c/h/u/n | 1 生字 |
| 5. 拒识端到端 + 前缀反馈 | 「冬」画横线 / 写错字母 x | 2 用例 |

## 通过 / 失败统计

| 验收点 | 结果 | 判定 |
|---|---|---|
| 1. 单字母准确率 | **86.54%**（135/156）｜Arial 100%（78/78），Georgia 73.08%（57/78） | ❌ 未达 ≥90% |
| 2. 候选集约束（含混淆对） | 约束命中 86.54%（135/156）＝无候选水平（测试集含最强混淆对，天然无法提升）；排除混淆对后 i 案例 0%→100% | ⚠️ 机制正确，条件性有效 |
| 2b. 回退保底 | 候选集不含真字母时 84.62%（132/156）返回真字母；80.77% 触发 lowConf | ✅ 符合设计 |
| 3. 拒识 | **0/5 拦截**：横线→j(0.35)、竖线→l(0.84)、斜线→l(0.76)、圆点→o(1.00)、乱画→h(0.70) | ❌ **拒识失效** |
| 4. 端到端通关 | c→h→u→n 逐字母识别正确 →「🎉 你真棒！拼音正确！」→ 1s 后蒙层关闭 → 卡片 .done | ✅ |
| 5. 拒识端到端 | 「冬」画横线 → 被采纳为字母 i，未出现「✍️ 没看清，再写一次」 | ❌ 复现 bug |
| 5b. 前缀反馈 | 「冬」写 x →「❌ 第1个字母不对，点字母上的✕重写」；「春」首字母错 → 同提示；前缀正确时无提示 | ✅ |
| 延迟 | 单次推理平均 **1.7ms**（目标 ≤100ms）；模型加载（缓存后）339ms | ✅ |
| ?allowed=da 快速模式 | 6 样本全部正确（无候选/候选一致，conf ≥0.93） | ✅ |

## 混淆矩阵摘要（最易混字母对）

| 排名 | 真字母 → 误识 | 次数(×6) | 置信度 | 备注 |
|---|---|---|---|---|
| 1 | h → b | 3 | 0.97 | 模型自信地错（Georgia 字形） |
| 1 | i → j | 3 | 0.59 | 候选集排除 j 后可修复 |
| 1 | k → b | 3 | 0.81 | 自信地错 |
| 1 | l → i | 3 | 0.83 | 自信地错（衬线脚像 i 的点） |
| 1 | m → a | 3 | 0.74 | 自信地错 |
| 1 | n → d | 3 | 0.80 | 自信地错 |
| 1 | r → i | 3 | 0.99 | 自信地错 |

全部 21 个失败样本均为 **Georgia 衬线体**（3 个抖动变体完全一致）；Arial 打印体 78/78 全对。真实手写（细笔画、无衬线）与 Arial 更接近，实际场景准确率预计显著高于 86.5%，但衬线/圆角字体是模型盲区。

## 失败案例分析

**案例 A（严重）：拒识完全失效 — 置信度取错位置**
- 现象：横线/竖线/圆点/乱画 5 种垃圾输入全部被采纳为字母（conf 0.35~1.00）。
- 定位：`index.html:878-881` 回退路径 `lowConf = true` 后返回的 `confidence` 是**全局 26 类最大概率**（如竖线→l 0.84），而 `doHWRecognize`（`index.html:964`）只检查 `result.confidence < HW_MIN_CONF`，**从未使用 `lowConf` 标志**。清晰笔画的全局最大概率几乎都 ≥0.30，拒识阈值形同虚设。
- 证据链：测试页 10/10 判定采纳 → E2E「冬」横线被识别为 i 并追加 chip（本应提示重写）。
- 修复方向：`doHWRecognize` 拒识条件改为 `if (result.lowConf || result.confidence < HW_MIN_CONF)`；更优做法是回退时让 `recognizeLetterCNN` 返回**候选集内最大概率**作为 confidence（此时 lowConf 必为 true），让拒识阈值真正反映「候选集内的把握」。

**案例 B（中）：回退保底可输出候选集之外的字母并被采纳**
- 现象：乱画输入 allowed=[c,h,u,n] → 返回 h(0.70, lowConf=false) 被采纳。
- 定位：allowed 内最大概率 ≥0.30 时直接采纳该字母（`index.html:875`），乱画恰好激活了 h。候选集约束只对「形似」输入有效，对「恰好命中候选集」的垃圾无能为力。
- 修复方向：与案例 A 同一处（lowConf/候选集内置信度判定），另可加多帧投票或最小笔画数启发式。

**案例 C（低）：模型对笔画粗细/字体敏感**
- 现象：bold 字重使 c→g（0.93）、72px bold 同样翻转；Georgia 全字母 21/21 失败样本中 7 个字母自信错误（0.74~0.99）。
- 定位：EMNIST 训练分布为细笔画手写；预处理仅做包围盒+缩放，无笔画粗细归一（AGENT.md 方向 A4 未实施）。
- 修复方向：方向 A4 笔画加粗/细化归一；方向 A3 孩子笔迹增强重训（尤其衬线/圆角变体）。

## 结论

| 指标（AGENT.md 4.2） | 目标 | 实测 | 达标 |
|---|---|---|---|
| 单字母准确率（规范书写） | ≥95% | Arial 口径 100%；总体 86.54%（含衬线压力测试） | ⚠️ 条件达标 |
| 端到端拼音通关 | ≥90% | 1/1 生字通过（样本少，说明性） | ⚠️ 待扩样 |
| 识别延迟 | ≤100ms | 1.7ms | ✅ |
| 无拒识误判（乱画被当正确字母） | 有阈值拦截 | **5/5 垃圾输入被采纳** | ❌ |
| 候选集约束 / 回退保底 / 前缀反馈 | 生效 | 约束条件性生效、回退符合设计、前缀反馈生效 | ✅ |

**结论：本轮优化（fc5d0f2）核心功能「置信度拒识」存在逻辑缺陷未生效，不予通过；候选集约束与前缀反馈功能验收通过。** 拒识修复（1 行条件改动）后需回归验收。

## 下一步建议

1. **必修**：修复拒识（案例 A）— 采纳 `lowConf` 或回退返回候选集内置信度；回归 5 种垃圾输入 + 真实手写。
2. 候选集约束对「模型自信错误」无效（h→b 0.97 等）——需方向 A4 笔画归一 / A3 重训才能根治，候选集只是第一道防线。
3. 端到端通关率扩样：按 AGENT.md 4.3 PINYIN_COMBO 抽 ≥10 生字（含易混淆 li/ni/lu/nu 等）跑真实浏览冒烟。
4. 拒识阈值调优时注意挫败感（AGENT.md 六-4）：建议先验证低置信分布再定 0.30 是否合适。

## 附：复现命令

```bash
./start.sh 3000   # 若端口被占自动后移
# 像素级测试：http://localhost:<port>/test/recognition-test.html
# 快速单字母：http://localhost:<port>/test/recognition-test?allowed=da
# E2E L1 自动切瓜：browse eval test/e2e-l1-slice.js
```

---

## 回归验收（拒识修复 fc5d0f2+1 行条件改动：`result.lowConf || confidence < HW_MIN_CONF`）

| 项 | 结果 |
|---|---|
| 测试时间 | 2026-07-31（主 Agent 执行，browse 真浏览器 :8080，识字1「飞 fei」） |
| 圆点（模型→o 1.0，不在候选集） | ✅ 拒识「✍️ 没看清，再写一次」，chips 不变 |
| 乱画涂鸦（模型→h 0.70，不在候选集） | ✅ 拒识，chips 不变 |
| 横线（模型→i 0.84，**在**候选集 {f,e,i}） | ⚠️ 采纳为 'i' + 「❌ 第1个字母不对」前缀反馈（候选集内含高置信即采纳，属模型 i/l 混淆固有边界，非逻辑缺陷） |
| 竖线（模型→l/i 高置信，候选集含 i） | ⚠️ 同上，采纳 + 前缀反馈 |
| 成功路径 f→e→i | ✅ 「🎉 你真棒！」→ 蒙层自动关闭 → 卡片 .done（1/1） |
| 修复有效性 | ✅ lowConf 拒识路径（点/涂鸦）生效；候选集内高置信仍采纳（设计如此，给出明确前缀错误提示） |

**结论：拒识修复达标。** 垃圾输入中「映射到候选集外字母」的全部被拦截；「映射到候选集内字母」的（i/l 混淆）给出明确的第 N 位错误提示而非静默判对。遗留：i/l 与衬线字形自信错误（c→g 0.93、h→b 0.97）需方向 A4/A3（预处理归一 / 模型重训）根治。

---

## 迭代2 验收（方向 A3 重训 + A4 预处理，commit 见下）

| 项 | 结果 |
|---|---|
| 测试时间 | 2026-07-31（主 Agent 执行：python 训练管线 + browse 真浏览器 :8080） |
| 训练数据 | EMNIST letters 124800 + 4 款 macOS 字体渲染 39936（Arial/Georgia/Chalkboard/BradleyHand × 26字母 × 16变体 × 24重复，重复系数≈3:1） |
| 增强 | 旋转±25°/缩放0.75-1.25/平移±3px（画布约束）/dilate-erode 粗细各25%/随机擦除/微调阶段低强度3epoch |
| 模型 | conv32-BN-MP-conv96-BN-MP-fc128-fc26，63.4万参数，onnx 2.54MB（≤4MB ✓），动态batch，opset13，Softmax，BN手工折叠导出 |
| 一致性 | torch vs onnxruntime max diff 1.56e-6 ✓ |
| EMNIST test | **新 92.49% vs 旧 17.55%**（旧模型纯字体训练，完全没学过真手写） |
| 字体（EMNIST质心对齐预处理） | 新：Arial/Georgia/Chalkboard/BradleyHand **全部 100%**；旧：100/80.8/100/88.5 |
| 字体（旧bbox预处理对照） | 新：100/100/96.2/80.8（新预处理对 Chalkboard/Bradley 提升明显） |
| 浏览器像素级（新模型+新预处理） | **156/156 = 100%**（26字母×2字体×3抖动），混淆矩阵全零，延迟 0.8ms |
| E2E 游戏内 | 入(rù)→r,u 通关✓；冬(dōng) 斜线→拒识「✍️ 没看清」✓；d,n 错误→「❌ 第2个字母不对」✓ |
| 垃圾输入映射（新模型） | 横线→l 0.67、竖线→l 0.64、斜线→x 1.00、圆点→o 0.96、乱画→z 0.49（候选集外被拒，候选集内走前缀反馈） |

**结论：达标。** 方向 A3+A4 完成，全指标优于旧模型：真手写从 17.55%→92.49%（5.3x），字体从最高 92.3%→100%。剩余弱项：i/l（Georgia i 仅 0.60）、y/v（y 0.73）、斜线→x 高置信（候选集兜底）。

## 下一步建议

1. 收集真实孩子手写样本加入训练（当前字体是代理分布）
2. 冷启动模型加载 5~30s 优化（方向 D2：onnx 量化 int8 / 预加载策略）
3. 候选集内加权混淆先验（i↔l、y↔v）

## 迭代3：真实手写笔画回放验收（2026-07-31）

### 背景
用户实测反馈"效果非常差"。此前验收全用字体 fillText（假测试）。本轮构建忠实测试台：
`test/hw-overlay-test.html`（镜像 index.html 蒙层逻辑 + 真实 pointer 事件回放 624 个 EMNIST 真手写笔画轨迹）。

### 根因（实测定位）
1. **细笔致大面积拒识**：旧笔宽 `max(4, w*0.012)` 在 iPhone 360px 画布仅 4.3px，降采样到 28×28 后 ≈0.5px 淡墨 → 手机画布实测 63.8%、拒识 36%
2. **i/j 点丢失**：EMNIST 成人笔迹 i/j 常无点/点连主干，骨架化后与 l 无法区分（i→l 21/24 高置信误识）
3. **sim-to-real 缺口**：模型在 EMNIST 位图 92% 但笔画渲染仅 78%（骨架折线+圆头笔画渲染与位图分布不同）

### 修复
1. 笔宽 `max(8, min(w,h)*0.05)` ≈ 18px（iPhone），匹配 EMNIST 2.5px/28×28
2. `add_ij_dots.py`：i/j 无点样本 80% 概率合成点（儿童书写规范）
3. `StrokeDataset`（笔画渲染训练数据）+ 弱字母过采样 + 笔画级形状变形（deform_strokes：弯曲量/闭合空隙/尖圆角/连笔）

### 实测结果（浏览器回放，手机画布 360×420，单字母候选集最严苛场景）

| 版本 | 准确率 | 拒识 | 错识 |
|------|--------|------|------|
| 旧模型+旧细笔（用户反馈时） | 63.8% | 113/312 (36%) | 0 |
| 新模型+粗笔 | 83.7% | 51/312 (16%) | 0 |
| +点修复 | 87.2% | 40/312 (12.8%) | 0 |
| +变形+过采样（最终） | **88.1%** | **37/312 (11.9%)** | 0 |

- 离线验收（同 624 样本）：EMNIST 92.82%、笔画渲染 88.0%（18px）/ 86.5%（4.3px）、字体 98.1%（y→v×3 残余）
- 拒识残余集中：d/r/b/q/g/h/i/f（各 2-4 个），碗状字母组（a/b/d/g/o/p/q）为最大混淆簇
- 旧模型对照：笔画渲染仅 15.4%（纯字体训练，无法识别真实手写）

### 经验教训
1. **字体 fillText 测试 ≠ 真实手写**：字体实心字形抗细化，真实笔画细线分布完全不同，必须用笔画回放验收
2. 画布 CSS 中 `flex:1`（flex-basis:0%）会覆盖 `width`，需 `flex:none` + 高特异性选择器（`#hw-overlay #hwCanvas`）
3. 多笔画字母的多个 pointerup 定时器会竞态（部分识别+空画布识别），测试台记录 conf 时只在非 null 时覆盖
4. 单点笔画（i/j 的点）在浏览器回放中必须加微移（0.4px），否则 pointerdown→up 无 move 事件不产生墨迹
5. 笔宽应与字形高度而非画布宽度成比例（EMNIST 笔画 ≈ 字形高的 10%）

## 下一步建议

1. 收集真实孩子手写样本加入训练（当前 EMNIST 成人 + 合成变形是代理）
2. 碗状字母组候选集内混淆先验加权（a/b/d/g/o/p/q 互混）
3. 冷启动模型加载 5~30s 优化（方向 D2：onnx 量化 int8 / 预加载策略）
4. 拒识阈值 0.30→0.20 可测（保持错识 0 的前提下救回 0.2~0.3 区间的拒识）

## 迭代4：真实游戏复现与修复（2026-07-31，commit 5ec0e9c + test 同步）

### 用户反馈
实际玩游戏到拼音关，拼音书写识别率"非常低"（与测试台 88.1% 不符）。

### 复现过程（browse 真实游戏路径）
1. 重置存档 → 开始游戏 → L1 切瓜 → 进入第二关 → 打开蒙层
2. 用真实 pointer 事件 + 真实停顿（笔画间 700ms，模拟小孩书写）回放 EMNIST 轨迹

### 根因 1：onnxruntime-web CDN 被墙 → 模型加载失败静默（致命）
- `ort-wasm-simd-threaded.jsep.mjs` 从 jsdelivr 加载 **164s+**，session 创建失败
- `recognizeLetterCNN` 返回 null → doHWRecognize **静默 return**（无任何反馈，用户写了没反应）
- 用户实测"识别率非常低"= 识别引擎根本没起来
- **修复**：vendor 本地化（vendor/ort 37MB + vendor/jieba 4MB），模型 3s 加载
- **技术坑**：① `ort.env.wasm.wasmPaths` 必须绝对 URL（相对路径 `new URL()` 解析失败 → 拼接出错 404）；② 动态 import 相对 specifier 必须 `./` 前缀（裸 specifier 直接 resolve 失败）；③ jsep.mjs 是 ESM，从 blob URL import 时 `import.meta.url` 失效

### 根因 2：400ms 停笔竞态 → 多笔画字母被拆碎误识
- 小孩写多笔画字母（t/i/f/k/u/g 等 2-3 笔）笔画间停顿 >400ms → 第一笔被单独识别
- **实测复现**：700ms 笔画间隔下 3 笔画 u 样本 #6→a、#7→a、#10→g（40ms 间隔全 OK）
- 根因：每次 pointerup 都启动独立 endDraw 窗口（400ms），并发窗口叠加 → 前一笔提前到期
- **修复**：单一 `hwRecTimer`（900ms）——pointerup 重置、pointerdown 取消，无并发
- 修复后 700ms 真实停顿模拟：**ru ✓ you ✓ shuang ✓** 全部通过（g 拒识 2 次后重试成功）

### 修复 3：识别引擎未就绪不再静默
- `if (!result)` 分支：引擎加载中 → 提示"⌛ 识别引擎加载中，请稍等片刻再试…"
- 加载失败 → 提示"⚠️ 识别引擎加载失败，请刷新重试"

### 修复 4：预加载
- 进入第二关（startLevel2）时提前 loadCNNModel()，打开蒙层即可用

### 回归验收（900ms 滑动停笔 + 1200ms 字母间隔，312 样本单字母候选集）
| 版本 | 准确率 | 拒识 | 错识 |
|------|--------|------|------|
| 迭代3（400ms 停笔） | 88.1% | 37/312 (11.9%) | 0 |
| 迭代4（900ms 滑动） | **88.14%** | 37/312 (11.9%) | 0 |

- 单字母准确率与迭代3持平（本轮修复时序/加载，不改变模型能力）
- 真实场景收益：引擎可加载 + 多笔画不碎 + 拒识可重试 → 端到端 ru/you/shuang 全过

### 经验教训
1. **CDN 禁令**：jsdelivr 在国内不可用（164s+ 且失败），onnxruntime/jieba 全部本地 vendor
2. **wasmPaths 必须绝对 URL**；动态 import 相对路径必须 `./` 开头
3. **停笔识别必须单一定时器**（up 重置/down 取消），多个并发 endDraw 窗口会把多笔画字母拆碎（u→a/g 实测）
4. **测试台批量间隔要 > 识别窗口**：800ms → 1200ms（否则相邻字母叠墨合并识别）
5. **验证要覆盖真实时序**：40ms 快速回放无法暴露 700ms 停顿下的碎笔 bug，必须用真实间隔模拟

## 下一步建议

1. 碗状字母组候选集内混淆先验加权（a/b/d/g/o/p/q 互混，g 58% 最弱）
2. 收集真实孩子笔迹（收益最大）
3. 拒识阈值 0.30→0.20 可测（救回 0.2~0.3 拒识，保持错识 0）
4. iPhone 真机验证（Safari + 触摸事件 + 弱网加载）
